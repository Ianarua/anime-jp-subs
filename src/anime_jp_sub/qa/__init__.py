# -*- coding: utf-8 -*-
"""质量工具（QA）：给"人工评审 → 调参 → 验收"这条流程用的东西。

**这些工具只在 `dev` 分支上，用户版（`main`）不包含** —— 用户只是想看带注音的字幕，
不需要评审和打分。

分两块（输入输出完全不同，所以一行一包）：
  * `review/` —— 产东西：把一集的日语字幕列成网页/清单，边看边标「哪句断错 / 哪句漏了」
  * `score/`  —— 收东西：把人工标出来的结果当**验收标准**，给切分算法打分
    （断句算法就是靠这套从"一处都不对"改到"对 44/80"的，见 AGENTS.md）
  * `common.py` —— 两边共用的一小块（读字幕正文、时间解析）

设计原则：**只读**——不碰 mkv、不改字幕，产物写在旁边或临时目录里，删掉即还原。
"""

__all__ = ["common", "review", "score"]
